{
  "name": "chalend",
  "version": "1.0.0",
  "main": "./build/server.js",

  "scripts": {
    "preinstall": "nvm use",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "license": "ISC",
  "author": "Felipe C. Trevisol",
  "description": "A Simple Chalend CRUD, using AWS Lambda.",
  "keywords": ["aws", "lambda", "simple node", "typescript"],

  "devDependencies": {
    "typescript": "^5.6.2"
  }
}